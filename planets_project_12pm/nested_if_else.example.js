let factorUnit = "repetitions";

if (factorSystem === "metric") {
    // if jump cm
    // if weight kg
}
else if (factorSystem === "imperial") {
    // if jump in
    // factorUnit = "in"
    //if weight lbs
 }